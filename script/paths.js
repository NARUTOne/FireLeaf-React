/**
 * config paths
 */

'use strict';

// const path = require('path');
// const resolvePath = (relativePath) => path.resolve(__dirname, relativePath);

const  proxyTable = {}; // 代理api

module.exports = {
  buildPath: 'dist', // 打包文件path
  publlicPath: '/', // 资源path
  PName: 'app', // 项目名称
  proxyTable 
};